<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mentorply">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".ParseApplication"
        android:allowBackup="true"
        android:icon="@drawable/mentorply_logo_background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mentorply_logo_background"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ChatActivity"></activity>
        <activity android:name=".activities.pairing.AcceptRequestActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.codepath.fileprovider.Mentorply"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/fileprovider" />
        </provider>

        <activity android:name=".activities.pairing.PairingActivity" />
        <activity android:name=".activities.pairing.ChoiceProfileActivity" />
        <activity android:name=".activities.pairing.RequestsActivity" />
        <activity android:name=".activities.program.CreateProgramActivity" />
        <activity android:name=".activities.program.JoinProgramActivity" />
        <activity android:name=".activities.program.DetailedProgramsActivity" />
        <activity android:name=".activities.accounts.SignupActivity" />
        <activity android:name=".activities.MainActivity" /> <!-- <activity android:name=".LoginActivity" /> -->
        <activity android:name=".activities.accounts.LoginActivity">

            <!--  -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>